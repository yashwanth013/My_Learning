// Naming Conventions

// Camelcase
var firstName;
firstName = prompt("Enter Your First Name");
// PascalCase
let LastName;
LastName = prompt("Enter Your Last Name");
// SnakeCase
let middile_name;
middile_name = prompt("enter your middile name");

// Using Constant (const keyword)
const price = 99;

//Course Name (String)

var courseName;
courseName = prompt("Course Name");

// Card Number (Number)

var cardNumber = "1234567890";

// Payment Status (boolean)
var isPaid = true;

// Declaring a Variable
var modeofPayment;
modeofPayment = prompt("Mode Of Payment");

//Output
alert(
  firstName +
    middile_name +
    LastName +
    " successfuYaslly bought " +
    courseName +
    " using " +
    modeofPayment
);
